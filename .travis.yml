sudo: required

language: java

notifications:
    slack: ifrs-engsoftware3:z5WVXZtPYKLLFoNmpTwL24ty

services:
  -  docker
  
before_install:
  -  docker build -t fizzbuzz/junit .
  
script:
  -  cd my-app/
  -  ls -l
  -  pwd
  -  docker run fizzbuzz/junit /bin/sh -c "ls -l; pwd; mvn package; mvn clean test;"